{
    "type": "object",
    "uniqueItems": true,
    "additionalProperties": false,
    "properties": {
        "workOrderId": {
            "type": "string",
            "pattern": "^(0[x|X])?[0-9a-fA-F]+$",
            "error_msg":
                "Invalid data format for workOrderId"},
        "updaterId": {
            "type": "string",
            "pattern": "^(0[x|X])?[0-9a-fA-F]+$",
            "error_msg":
                "Invalid data format for updaterId"},
        "updateIndex": {
            "type": "integer",
            "error_msg":
                "Invalid data format for updateIndex"}
    }
}

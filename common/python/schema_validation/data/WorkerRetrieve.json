{
    "type": "object",
    "uniqueItems": true,
    "additionalProperties": false,
    "properties": {
        "workerId": {
            "type": "string",
            "pattern": "^(0[x|X])?[0-9a-fA-F]+$",
            "error_msg":
                "Invalid data format for Worker id"
            }
    },
    "required": ["workerId"]
}
